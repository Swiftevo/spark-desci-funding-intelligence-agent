# 2026-06-07 Progress Log

## Progress

### M1: Agent Core Complete

- Created `run-agent-review.ps1`, the GLM-5.1 function-calling agent loop.
- GLM-5.1 can choose tools autonomously instead of following a hardcoded script path.
- Test result for `DSPJ-0003`: 4 turns, multiple tool calls, structured review output.
- Agent workflow: get project detail, search related projects, compare projects, search academic context, synthesize review.
- This demonstrates the Z.AI long-horizon task pattern more clearly than a single-call summary.

### Tool Layer

| Tool | Purpose |
|------|---------|
| `search_projects` | Search the 49-project Spark dataset |
| `get_project_detail` | Fetch full project detail |
| `compare_projects` | Compare target and related projects |
| `search_academic_context` | Placeholder academic context adapter |

### M2: Batch Review Script Ready

- Created `run-batch-review.ps1`.
- Randomly selects projects and runs agent review for each.
- Includes progress display, error handling, delay between API calls, and summary output.
- Not yet executed as final demo evidence.

### Token And Reliability Improvements

| Improvement | Change | Why |
|-------------|--------|-----|
| Tool result compression | Remove empty fields and truncate long `raw_text` | Reduce token usage |
| Turn limit | `MaxTurns` reduced from 12 to 6 | Prevent runaway loops |
| Prompt schema | Removed example values while keeping field definitions | Save prompt tokens without losing format guidance |
| Comparison metadata | Added `target_domain`, `dominant_comparison_domain`, and `comparison_note` | Avoid misleading reviewers |

## Academic Context Limitation

This must be stated in README, demo remarks, and demo narration:

```text
1. The academic context returned by the tool is placeholder data.
2. GLM-5.1 analysis based on placeholder context is not real literature support.
3. The next step is replacing the placeholder with Semantic Scholar or AMiner retrieval.
```

Current demo truth:

```text
Spark project retrieval is real.
GLM-5.1 tool calling is real.
Cross-project comparison over Spark data is real.
Academic context is placeholder scaffolding.
```

## Next Steps

- [ ] Execute batch review with API key.
- [ ] Convert `DSPJ-0003-agent-review.json` into a polished reviewer brief.
- [ ] Create `DEMO_SCRIPT.md` using the agent trace as the 3-minute storyline.
- [ ] Prepare submission README and proposal text.
- [ ] Replace placeholder academic context with Semantic Scholar or AMiner when available.

## Risks

- Z.AI API quota should be monitored.
- AMiner API access is not confirmed yet.
- Demo must not imply that placeholder academic context is verified literature.
- Submission deadline is 2026-06-13 12:00 UTC+8.
